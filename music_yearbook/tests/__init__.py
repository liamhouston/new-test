"""Test package for music_yearbook.

Making this directory a package ensures Django/unittest discovery imports
tests as `music_yearbook.tests` rather than a top-level `tests` module.

For these tests to be discovered and run correctly, use:
python3.13 manage.py test music_yearbook.tests
"""
